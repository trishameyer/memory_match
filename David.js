$(function(){   flip();});//variables to keep track ofvar $fundingValue = $('#funding').next().html();var $accuracy = $('#accuracy').next();var $round = $('#round').next();var cardsFlippedSrcs = [];//you'll add one to this value when a card is flippedvar dataOfFlipped = [];//flip functionfunction flip() {    $('.card').click(function () { //on click        var firstDataFlip; //data attribute of first undefined        var imgSrc = $(this).find('img:first').attr('src'); //src attr        var imgData = $(this).find('img:first').attr('data'); //data attribute of current defined            if(cardsFlippedSrcs.length === 0){                $(this).addClass('clicked');                cardsFlippedSrcs.push(imgSrc);                firstDataFlip = imgData;                console.log(cardsFlippedSrcs);            }        //if only one card is flipped, and the current clicked cards src doesn't equal the image of the previous and the data attribute also doesn't equal the previous flipped            else if(cardsFlippedSrcs.length === 1) {                if((imgSrc !== cardsFlippedSrcs[0]) && (imgData !== firstDataFlip)) {                    $(this).addClass('clicked');                    cardsFlippedSrcs.push(imgSrc);                    console.log(cardsFlippedSrcs);                }            }    });}//function for a new game, randomize cards,function newGame() {    var $randGameboard = shuffle(srcArray); //make a new array of randomized src attributes    for (var i = 0; i < $randGameboard.length; i++){        var curSrc = $randGameboard[i]; //store the current index value of random array in a variable        //$imgArray is a jquery object that references an html node list.. when you index this.. it'll pul the value... make that value a jquery object        $($imgArray[i]).attr('src', curSrc);//make the index value a jquery object and change it's attr value to the random src index i    }}//array of images (back);var $imgArray = $('.back');//array of images(the function below makes this into the source array;var srcArray = [];//for each index value in the array of images ('.back'), push the attr 'src' into a new array. ^^$imgArray.each(function(){    srcArray.push($(this).attr('src'));});//shuffle functionfunction shuffle(array){    var i = array.length;    var rand;    var temp;    while(--i>0){        rand = Math.floor(Math.random()*(i+1));        temp = array[i];        array[i] = array[rand];        array[rand] = temp;    }    return array;}//shuffle the game board below..